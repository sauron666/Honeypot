# ADR-004 — Наблюдение на примамките: agentless VMI, не in-guest агент

**Статус:** приет · **Дата:** 2026-08-28

## Контекст
Нужна ни е дълбока телеметрия от примамката (процеси, файлове, registry, крипто API).
Стандартният подход на конкуренцията е агент в госта или само логове на услугата.

## Разгледани опции
1. **In-guest агент** — лесно, богата телеметрия. Но: атакуващият го вижда в process
   list, може да го убие, може да го фингърпринтне → рушим стълб №1 (undetectable).
2. **Само логове на услугата** — незабележимо, но плитко; не виждаме какво прави
   атакуващият след експлоатация.
3. **VMI през libvmi/DRAKVUF** — наблюдение отвън, невидимо за госта, дълбочина до
   syscall. Крехко при нови OS build-ове, изисква Rust/C работа, тежко за CPU.

## Решение
Опция 3, с интерфейс `HostObserver`, зад който може да стои и опция 1 (за среди,
където VMI не е възможно — vSphere, cloud) или eBPF за Linux примамки.

## Последствия
+ Единствената сериозна форензична дълбочина без следа в госта; ключово
  конкурентно предимство и предпоставка за ransomware key capture.
− Ограничава ни до KVM/Xen за пълната функционалност; изисква поддръжка на набор
  от известни OS build-ове (Rekall-подобен профил на ядрото).
− Значителна инженерна инвестиция (фаза 2, ~10 седмици).
